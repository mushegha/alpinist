import TickerStatus from '@/ticker-status'
import LadderStatus from '@/ladder-status'

const components = {
  TickerStatus,
  LadderStatus
}


export default {
  name: 'symbol-scope-view',
  components
}
