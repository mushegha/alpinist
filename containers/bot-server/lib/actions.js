const rt = require('rethinkdb')

const { curryN } = require('ramda')

// const Ladder = rt
//   .db('alpinist')
//   .table('ladder')

async function buy (params, slot) {
  // const data = merge(slot, params)
  // data.openTime = new Date()
  //
  // return Ladder
  //   .insert(slot)
  //   .run(conn)
}

async function sell (params, slot) {

}
